function output = rgb2yuv(input)

    
    he_vec = reshape(he_YUV, [], 1, size(he_double,3));
    R = he_vec(:,:,1);
    G = he_vec(:,:,2);
    B = he_vec(:,:,3);

    Y = 0.299 * R + 0.587 * G + 0.114 * B;
    U = -0.14713 * R - 0.28886 * G + 0.436 * B;
    V = 0.615 * R - 0.51499 * G - 0.10001 * B;
    
    he_vec
end